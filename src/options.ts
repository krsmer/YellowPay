// Options/Settings Script
console.log('YellowPay options page loaded');

document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app');
  if (app) {
    app.innerHTML = `
      <h1>YellowPay Settings</h1>
      <p>Settings will be implemented in Phase 5</p>
    `;
  }
});
